// Αντίγραψε αυτό το αρχείο σε config.js και βάλε το δικό σου Groq API key.
// Το config.js ΔΕΝ πρέπει ποτέ να ανέβει στο GitHub (είναι στο .gitignore).
window.APP_CONFIG = {
    GROQ_API_KEY: "your-groq-api-key-here"
};
